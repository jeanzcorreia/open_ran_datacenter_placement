"""SML_LLM — camada nova (src/) que encapsula o problema de placement de ODCs e os
otimizadores, sem reescrever o código original (odc_placement_parser.py).

Fase 2: src.problem (Instance + ODCPlacementProblem) e src.optimizers (base + nsga2).
"""
